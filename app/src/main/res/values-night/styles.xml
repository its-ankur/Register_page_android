<resources>
    <!-- Base application theme for dark mode -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/toolbar_color</item>
        <item name="colorPrimary">@color/toolbar_color</item>
        <item name="colorPrimaryDark">@color/toolbar_color</item>
        <item name="colorAccent">@color/toolbar_color</item>
        <!-- Other attributes specific to your app in dark mode -->
    </style>
</resources>
